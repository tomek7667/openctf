// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"openctfbackend/ent/team"
	"openctfbackend/ent/teamachievement"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TeamAchievement is the model entity for the TeamAchievement schema.
type TeamAchievement struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name"`
	// UnlockedAt holds the value of the "unlocked_at" field.
	UnlockedAt time.Time `json:"unlocked_at"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TeamAchievementQuery when eager-loading is set.
	Edges                 TeamAchievementEdges `json:"edges"`
	team_achievement_team *int
	selectValues          sql.SelectValues
}

// TeamAchievementEdges holds the relations/edges for other nodes in the graph.
type TeamAchievementEdges struct {
	// Team holds the value of the team edge.
	Team *Team `json:"team,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TeamOrErr returns the Team value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamAchievementEdges) TeamOrErr() (*Team, error) {
	if e.Team != nil {
		return e.Team, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: team.Label}
	}
	return nil, &NotLoadedError{edge: "team"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamAchievement) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamachievement.FieldID:
			values[i] = new(sql.NullInt64)
		case teamachievement.FieldName:
			values[i] = new(sql.NullString)
		case teamachievement.FieldUnlockedAt:
			values[i] = new(sql.NullTime)
		case teamachievement.ForeignKeys[0]: // team_achievement_team
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamAchievement fields.
func (_m *TeamAchievement) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamachievement.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case teamachievement.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case teamachievement.FieldUnlockedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field unlocked_at", values[i])
			} else if value.Valid {
				_m.UnlockedAt = value.Time
			}
		case teamachievement.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field team_achievement_team", value)
			} else if value.Valid {
				_m.team_achievement_team = new(int)
				*_m.team_achievement_team = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeamAchievement.
// This includes values selected through modifiers, order, etc.
func (_m *TeamAchievement) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTeam queries the "team" edge of the TeamAchievement entity.
func (_m *TeamAchievement) QueryTeam() *TeamQuery {
	return NewTeamAchievementClient(_m.config).QueryTeam(_m)
}

// Update returns a builder for updating this TeamAchievement.
// Note that you need to call TeamAchievement.Unwrap() before calling this method if this TeamAchievement
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TeamAchievement) Update() *TeamAchievementUpdateOne {
	return NewTeamAchievementClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TeamAchievement entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TeamAchievement) Unwrap() *TeamAchievement {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TeamAchievement is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TeamAchievement) String() string {
	var builder strings.Builder
	builder.WriteString("TeamAchievement(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("unlocked_at=")
	builder.WriteString(_m.UnlockedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TeamAchievements is a parsable slice of TeamAchievement.
type TeamAchievements []*TeamAchievement
